import { Spinner } from 'react-bootstrap';

function BorderExample() {
  return <Spinner animation="border" />;
}

export default BorderExample;

export const borderExampleCode = `import { Spinner } from "react-bootstrap";

function BorderExample() {
  return <Spinner animation="border" />;
}

export default BorderExample;`;
