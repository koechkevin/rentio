import { ProgressBar } from 'react-bootstrap';

function BasicExample() {
  return <ProgressBar now={60} />;
}

export default BasicExample;

export const basicExampleCode = `import { ProgressBar } from 'react-bootstrap';

function BasicExample() {
  return <ProgressBar now={60} />;
}

export default BasicExample;`;
