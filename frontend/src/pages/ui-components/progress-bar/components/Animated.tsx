import { ProgressBar } from 'react-bootstrap';

function AnimatedExample() {
  return <ProgressBar animated now={45} />;
}

export default AnimatedExample;

export const animatedExampleCode = `import { ProgressBar } from "react-bootstrap";

function AnimatedExample() {
  return <ProgressBar animated now={45} />;
}

export default AnimatedExample;`;
